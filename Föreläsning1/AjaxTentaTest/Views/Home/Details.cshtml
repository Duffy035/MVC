@model AjaxTentaTest.Models.Person

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Person</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.BirthDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BirthDate)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new {id = Model.Id}) |
    @Html.ActionLink("Back to List", "Index")
</p>

@Ajax.ActionLink(linkText:"Klick me first! :-)",
    actionName:"HitMe",
    ajaxOptions: new AjaxOptions()
    {
        UpdateTargetId = "ajaxTarget",
        InsertionMode = InsertionMode.InsertAfter
})

<div id="ajaxTarget"></div>

@Ajax.ActionLink(linkText: "Klick me after! :-)",
    actionName: "Hide",
    ajaxOptions: new AjaxOptions()
    {
        UpdateTargetId = "ajaxTarget"       
    })


@Ajax.ActionLink(linkText: "Call an action, return PartialView",
    actionName:"GetCarCreate",
    routeValues: new {id = @Model.Id},
    ajaxOptions: new AjaxOptions()
    {
        UpdateTargetId = "ajaxPartialView",
        InsertionMode = InsertionMode.Replace
    })

<div id="ajaxPartialView"></div>

